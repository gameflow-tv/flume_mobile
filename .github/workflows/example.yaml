name: Example
on:
  push:
    branches: ["main", "example-build"]

jobs:
  build:
    name: build
    strategy:
      matrix:
        target: [ios, appbundle, macos, linux, windows]
    runs-on: ${{ matrix.target == 'windows' && 'windows-latest' || matrix.target == 'linux' && 'ubuntu-latest' || 'macos-latest' }}
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
        with:
          channel: beta
          architecture: x64
          cache: true

      - run: flutter config --enable-macos-desktop --enable-windows-desktop --enable-linux-desktop

      - id: args
        run: |
          echo "::set-output name=ios::--no-codesign"
          echo "::set-output name=appbundle::''"
          echo "::set-output name=macos::''"
          echo "::set-output name=linux::''"
          echo "::set-output name=windows::''"

      - if: matrix.target == 'appbundle'
        name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: "11.x"
          distribution: temurin

      - if: matrix.target == 'linux'
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ninja-build libgtk-3-dev

      - run: flutter pub get
        working-directory: example

      - run: flutter build ${{ matrix.target }} ${{ steps.args.outputs.${{ matrix.target }} }}
        working-directory: example
