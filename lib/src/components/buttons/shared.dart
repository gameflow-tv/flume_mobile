enum ButtonSize {
  medium,
  large,
}

enum ButtonVariant {
  primary,
  tonal,
  signal,
}

enum ToggleButtonVariant {
  standard,
  tonal,
}

enum ButtonState {
  normal,
  hover,
  pressed,
  focus,
  disabled,
  loading,
}
