enum ButtonSize {
  medium,
  large,
  expand,
}

enum ButtonVariant {
  primary,
  tonal,
  signal,
  light,
}

enum ToggleButtonVariant {
  standard,
  tonal,
}

enum ButtonState {
  normal,
  hover,
  pressed,
  focus,
  disabled,
  loading,
}

enum IconPosition {
  left,
  right,
}
